# AgentForge AI编程助手指导原则

本项目是 TypeScript 全栈 + AI 脚手架，面向业务功能开发，目标是像搭积木一样快速构建产品。

## 核心原则

### 1. 思考先于编码
不确定时提问而非猜测。存在歧义时呈现多种解释。有更简单方案时提出反对。困惑时明确指出并寻求澄清。

### 2. 简洁优先
最小代码解决实际问题。不添加超出要求的功能，不为单用途代码创建抽象，不添加未要求的"灵活性"或"可配置性"。

### 3. 精确修改
只触碰必须的部分。不要"改进"相邻代码或格式，不要重构未损坏的内容，匹配现有风格。你的更改产生的孤立代码自己清理。

### 4. 目标驱动
将任务转化为可验证目标。多步骤任务陈述简要计划，每步带验证标准。

## AgentForge 特定规则

**业务开发优先：** 支持业务功能开发而非构建 AI Agent 产品。代码结构应简单、直观、模块化，便于 AI 编程助手理解。

**Agent 开发约束：** 不为单一 Agent 创建基类，不为简单消息处理创建复杂状态机。使用 BaseAgent 和内置工具，不要重复造轮子。配置应简单可读，避免嵌套配置对象。

**集成测试：** 每个 Agent 需要覆盖主要用户场景的集成测试。每个自定义工具需要单元测试，覆盖边界情况和错误条件。关键工作流需要端到端测试。

## 项目规范

### 文件组织
```
src/
├── agents/          # Agent定义文件
├── tools/           # 自定义工具
├── components/      # UI组件
└── utils/           # 工具函数
```

### 命名约定
- Agent文件: kebab-case (`chat-agent.ts`)
- 类名: PascalCase (`ChatAgent`)
- 函数: camelCase (`sendMessage`)
- 常量: UPPER_SNAKE_CASE (`MAX_MEMORY_SIZE`)

### 代码风格
TypeScript 严格模式。导出函数/类需 JSDoc。优先使用命名导出和函数式编程。导入分组: 第三方库 → 内部模块 → 相对路径。

---

好的代码简单解决今天的问题，不过早解决明天的问题。
